// nextflow.config

// process {
//    executor = 'slurm'
//    queue = 'cpu'
//    clusterOptions = '--nodelist=edi01'
//    cpus = 10  
// }

// Disable conda since we're using an existing environment
conda.enabled = false