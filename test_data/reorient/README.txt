reorient_paired_end_reads.sh 
reorient_single_end_reads.sh

workflows in both scripts are FINISHED!
#LOOP NOT DONE, WHEN USING MULTIPLE PRIMERS

# file extension and primers need to be specified inseide the scripts; but need to remove for implementation in PipeCraft.
#Works with all the fastq (fastq or fasta files for reorient_single_end_reads.sh)
in the working dir.
#outputs written in reoriented_out


